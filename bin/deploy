#!/usr/bin/env bash

echo "${BASH_SOURCE[0]}"

git fetch
git reset --hard origin/master

chown -R flump:flump /home/flump/
systemctl daemon-reload
systemctl restart flump.service
systemctl status flump.service
