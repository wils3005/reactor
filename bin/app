#!/usr/bin/env ruby
# frozen_string_literal: true

puts __FILE__

dotenv =
  File.
  read('.env').
  split("\n").
  reject { _1 =~ /^\s*#/ }.
  map { _1.split('=') }.
  to_h

ENV.merge!(dotenv)

require_relative '../lib/service'

Service.new
